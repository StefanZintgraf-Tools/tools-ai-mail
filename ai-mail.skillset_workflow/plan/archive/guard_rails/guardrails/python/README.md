# Python Detail Modules

Use these files with `../python_guardrails.md`.
Each `PY-MUST-*` rule in the global file maps to one of these modules.

## Modules
- `typing_interfaces.md` (`PY-MUST-01`)
- `imports_structure.md` (`PY-MUST-02`)
- `error_and_resource_safety.md` (`PY-MUST-03`, `PY-MUST-04`)
- `function_design_and_state.md` (`PY-MUST-05`, `PY-MUST-06`)
- `documentation_and_todos.md` (`PY-MUST-07`, `PY-MUST-08`)
- `quality_gates.md` (`PY-MUST-09`)

